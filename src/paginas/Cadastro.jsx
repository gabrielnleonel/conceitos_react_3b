export default function Cadastro () {

    return (
        <div>
        <h1>cadastro</h1>
        <button onClick={() => navegar('login')}>já tenho cadastro</button>

        </div>
        
    )
}